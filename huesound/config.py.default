#!/usr/bin/env python

# default postgres connect string
PG_CONNECT = "dbname=huesound user=huesound"

# use this connect string if postgres is running on TCP/IP port 5432
#PG_CONNECT = "dbname=huesound user=huesound host=localhost port=5432"

# Oher configuration options
APP_NAME = "huesound-spotify"
APP_VERSION = "0.1"
USER_AGENT = APP_NAME + "/" + APP_VERSION
